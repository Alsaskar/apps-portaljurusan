import Layout from "./Layout";
// import { useUser } from "../../context/UserContext";

const DosenDaftarRPS = () => {
  // const { user } = useUser();

  return (
    <>
      <Layout />
    </>
  );
};

export default DosenDaftarRPS;
